Step : 

python -m venv venv
#make virtual environment in visual studio

.\venv\Scripts\Activate (windows)
source venv/bin/activate (linux)
#activate virtual environtment
#dengan menggunakan virtual environment  module2 akan di install didalam folder ini saja.

pip install imutils
#centroid tracker

pip install opencv-python
#opencv python

pip install scipy
#scientific Python is an open-source library used for scientific and technical computing in Python. It is built on top of NumPy, extending its functionality and providing a wide range of efficient algorithms for numerical integration, optimization, interpolation, signal processing, linear algebra, and statistics, among other tasks.